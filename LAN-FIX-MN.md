# LAN / WebSocket засвар — 2026-09-12

2026-09-12.1 шинэчлэл суусны дараа IP өөрчлөгдөж `/_next/hmr` WebSocket алдаа гарсан тохиолдлын нэмэлт засвар.

Өмнөх next.config.js-д 10.255.85.198 тогтмол үлдсэн байсан. Серверийг 0.0.0.0 дээр асааж шинэ 10.245.187.198 хаягаар ороход хөгжүүлэлтийн Origin зөвшөөрөгдөхгүй байв. Шинэ файл сервер асах бүрдээ компьютерийн одоогийн IPv4 хаягуудыг танина. Бүх гаднын хаягийг зөвшөөрсөн wildcard ашиглаагүй.

## Зөвхөн нэг файл хуулах

1. Сервер ажиллаж байгаа хар цонхонд Ctrl+C, асуувал Y → Enter.
2. kino-lan-fix-20260912.zip-ийг задлана.
3. Доторх next.config.js файлыг одоогийн төслийн package.json-тай зэрэгцээ байрлуулж Replace хийнэ:

```text
D:\New folder (3)\Downloads\mgldrama2-reviewed\mgldrama2-reviewed
```

4. Админ нэвтрэхэд «Хүсэлтийн эх сурвалж буруу» алдаа гарахаас сэргийлж `.env.local` дотор SITE_URL мөрийг одоогийн IP-ээр тохируулна. Байгаа мөрийг солино, байхгүй бол нэг удаа нэмнэ:

```dotenv
SITE_URL=http://10.245.187.198:3000
```

Ctrl+S дарж хадгалаад төслийн PowerShell цонхонд:

```powershell
npm run dev -- --hostname 0.0.0.0 --port 3000
```

5. Ready гарсны дараа браузерт Ctrl+F5 дарна. Одоогийн сайтын хаяг [http://10.245.187.198:3000](http://10.245.187.198:3000).

Энэ нэг файлын засварт npm install эсвэл SQL дахин ажиллуулах шаардлагагүй. /api/health-ийн version 2026-09-12.1 хэвээр. Дахин IP өөрчлөгдвөл серверээ зогсоож асаана; SITE_URL болон MacroDroid-ийн URL дахь IP-ийг мөн тухайн үеийн хаягаар солино. Компьютер, утас хоёул SITE_URL-д бичсэн нэг хаягаар орно. 0.0.0.0-ийг браузер эсвэл MacroDroid-ийн URL болгохгүй.

Хэрэв WebSocket алдаа арилсан ч кино удаан ачаалсаар байвал серверийн хар цонхны /api/db хариу болон алдааны зургийг шалгана. Энэ тохиргоо дангаараа өгөгдлийн сангийн холболт зөв гэдгийг батлахгүй.

Зөвхөн интерфейсийн мэдээлэл унших эрхгүй тусгай хостод DEV_ALLOWED_ORIGINS=10.245.187.198 гэх мэт өөрийн бодит IPv4 хаягийг development environment-д өгч болно. Windows дээр ердийн үед автоматаар танина. Production-д энэ dev тохиргоо Origin болон төлбөрийн эрхийн шалгалтыг сулруулахгүй.

## Шалгасан нь

Суусан Next.js 16.3.4-ийн баримт, блоклох кодоос шалтгааныг тулгасан. Өмнөх allowlist шинэ IP-г хориглож байгааг давтаж үзсэн. Өөрчлөгдсөн интерфейсийн мэдээллийг загварчилж auto-detection болон мэдээлэл унших эрхгүй үеийн startup-ыг шалгасан. Бодит локал Next dev сервер дээр шинэ LAN Origin-тай WebSocket handshake 101 болж, гаднын Origin хориглогдсоныг шалгасан. Таны Windows дээрх эцсийн үр дүнг refresh хийж баталгаажуулна.

Нэвтрэлтийн Origin шалгалтыг мөн бодит локал Next dev сервер дээр туршсан: 0.0.0.0 bind + хоосон SITE_URL үед LAN POST 403 болж, яг LAN SITE_URL өгсний дараа cookie-гүй logout хүсэлт 200 болсон; гаднын Origin 403 хэвээр. Нууц үг, бодит session, өгөгдлийн санг энэ туршилтад ашиглаагүй.
