name: Verify package card copy
on:
  push:
    branches: [feature-package-card-copy]
    paths:
      - 'scripts/patch-package-card-copy.mjs'
      - '.github/workflows/feature-package-card-copy.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: feature-package-card-copy
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22'
          cache: npm
      - name: Apply requested package copy
        run: node scripts/patch-package-card-copy.mjs
      - name: Install dependencies
        run: npm ci
      - name: UI tests
        run: npm run test:ui
      - name: Unit tests
        run: npm test
      - name: Typecheck
        run: npm run typecheck
      - name: Production build
        run: npm run build
      - name: Diff safety
        run: git diff --check
      - name: Commit verified change
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/components/FilmLanding.tsx tests/ui/movie-details.test.cjs
          git commit -m 'Move 60 movie access copy into package cards'
          git push origin HEAD:feature-package-card-copy
