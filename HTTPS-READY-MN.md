# HTTPS бэлтгэл — 2026-09-12.1

Энэ багц код болон тохиргоог бэлдсэн. Бодит серверт deploy, DNS өөрчлөлт, сертификат үүсгэлт, Supabase SQL ажиллуулах үйлдэл хараахан хийгдээгүй.

IP солигдсоны дараах WebSocket засвар багтсан. Өмнөх 2026-09-12.1 хувилбарыг аль хэдийн суулгасан бол LAN-FIX-MN.md-ийн дагуу зөвхөн next.config.js-ийг шинэчилж болно.

## Одоо компьютер дээрээ суулгах

1. Ажиллаж байгаа серверийн хар цонхонд Ctrl+C, асуувал Y дарж зогсооно.
2. Одоогийн төслийн хавтсаа бүтнээр нь нөөцөлнө. `.env.local` файлтайгаа үлдэх ёстой.
3. Шинэчлэл ZIP доторх `app`, `lib`, `scripts`, `supabase`, `tests`, `public`, `deploy` хавтас болон үндсэн файлуудыг одоогийн **package.json байгаа хавтас** руу хуулж давхардуулна. Нэмэлт давхар `app/app` хавтас үүсгэхгүй.

Өмнөх зурагт ажиллаж байсан үндсэн хавтас:

```text
D:\New folder (3)\Downloads\mgldrama2-reviewed\mgldrama2-reviewed
```

4. `POSTER-SETUP-MN.md`-ийн дагуу SQL-ээ нэг удаа ажиллуулна.
5. Төслийн үндсэн хавтсанд нээсэн PowerShell/Terminal дотор:

```powershell
npm install
npm test
npm run build
npm run typecheck
```

6. Компьютер/утасны LAN туршилтад `.env.local` дотор SITE_URL мөрийг одоогийн сайтын хаягаар тохируулна. Мөр байгаа бол солино, байхгүй бол нэг удаа нэмнэ:

```dotenv
SITE_URL=http://10.245.187.198:3000
```

Ctrl+S дарж хадгалаад серверээ асаана:

```powershell
npm run dev -- --hostname 0.0.0.0 --port 3000
```

7. Сайтаа Ctrl+F5 дарж шинэчилнэ. `http://10.245.187.198:3000/api/health` нээхэд `version: 2026-09-12.1` байх ёстой. Энэ нь шинэ код ачаалсныг л харуулна; төлбөрийн бүрэн шалгалт биш. 10.245.187.198 нь одоогийн компьютерийн IP; өөрчлөгдвөл ipconfig-оос Wi-Fi/Ethernet-ийн IPv4-ийг харна. 0.0.0.0-ийг сайтын URL-д бичихгүй. Компьютер, утас хоёул SITE_URL-д бичсэн нэг хаягаар орно. IP солигдвол SITE_URL болон MacroDroid-ийн URL-ийг шинэчлээд серверээ дахин асаана.

INSTALL.cmd ажиллуулах шаардлагагүй; шинэ ZIP-д суурилуулагч скрипт оруулаагүй. Windows-ийн хамгаалалтыг унтраахгүй. Бүрэн эх кодын ZIP болон шинэчлэл ZIP-ээс нэгийг нь сонгоно. Хоёуланд одоогийн ажиллах код багтсан; шинэчлэл ZIP-д хуучин хувилбарын тайлбаруудыг оруулаагүй.

## Хуучин төлбөрийн trigger болон RPC-г хаах — нэмэлт засвар

Хэрэглэгчийн SQL export-оор хуучин auto_confirm_payment() нь sms_logs-д status=confirmed, ref_code таарсан мөр нэмэгдэхэд дүн/хугацаа шалгахгүй pending захиалгыг баталгаажуулж, confirmed_at бичихгүй байсныг тогтоосон. confirm_payment(text) нь төлөв шалгахгүй тул revoked захиалгыг дахин баталгаажуулах, confirmed захиалгын эхлэх цагийг сунгах боломжтой кодтой байв. Энэ нь хуучин кодын үйлдлийн дүгнэлт; бодит сангийн төлбөр буруу өөрчлөгдсөн түүхийг тогтоосон гэсэн үг биш.

Одоогийн /api/sms эдгээрийг дууддаггүй, sms_logs-д бичдэггүй. SMS нууц, банкны илгээгч, орлогын дүн, 6 оронтой код, pending төлөв, захиалгын насыг шалгасны дараа pending_payments-ийг шууд PATCH хийдэг.

Өмнөх SQL-ийг суулгасан байсан ч **энэ шинэчилсэн supabase/LEGACY-FUNCTIONS-LOCK.sql**-ийг SQL Editor → New query руу бүтнээр нь хуулж Run дарна. Шинээр суулгах хамгийн сүүлийн HTTPS-UPGRADE.sql-д мөн нэгтгэсэн. Зөвхөн энэ нэмэлт SQL-г суулгахад аппын файлуудаа дахин хуулах, сервер дахин асаах шаардлагагүй.

Шинэ SQL:

- public.sms_logs дээрх яг trigger_confirm_payment-ийг идэвхгүй болгоно. Түүний нэр/функц/хүснэгт шалгасан тодорхойлолтоос зөрвөл өөрчлөлтөө буцааж, шалгах алдаа өгнө.
- auto_confirm_payment(), confirm_payment(text)-ийн EXECUTE эрхийг PUBLIC, anon, authenticated, service_role-оос хаана. Энгийн серверийн дуудлага хуучин төлбөрийн замыг ашиглахгүй болно.
- get_film_cats(), update_film_cat(bigint,text)-ийн нийтийн эрх хаалттай, service_role эрхтэй хэвээр байна.
- Функцийн код, trigger-ийн тодорхойлолт, кино, хэрэглэгч, захиалга, SMS мөрүүдийг устгахгүй, төлбөрийн төлөв өөрчлөхгүй. Бусад trigger-ийг идэвхгүй болгохгүй. DB owner нь цаашид зориудаар өөрчлөх бүрэн эрхтэй хэвээр.

Results дэх хүлээгдэж буй утга:

| function_name | anon_execute | authenticated_execute | server_execute | trigger_state |
|---|---|---|---|---|
| auto_confirm_payment() | false | false | false | disabled |
| confirm_payment(text) | false | false | false | - |
| get_film_cats() | false | false | true | - |
| update_film_cat(bigint,text) | false | false | true | - |

Өмнөх файл бүх дөрвөн функцэд server_execute=true үлдээж байсан. Төлбөрийн хоёр хуучин функцийн бодит кодыг үзсэний дараа тэдгээрийн серверийн эрхийг хаахаар энэ хувилбарыг шинэчилсэн. Хуучин татсан файлыг дахин суулгахгүй; хамгийн сүүлийн файлыг ашиглана.

Дараа нь MacroDroid-ийн бодит /api/sms дамжуулалтыг нэг захиалгаар шалгана: зөв код/дүнтэй орлого дээр ok=true, хэрэглэгчийн эрх нээгдэх; давтан SMS эрхийн цагийг сунгахгүй байх. Бодит орлого баталгаажаагүй захиалгыг туршилт нэрээр confirmed болгож болохгүй.

## HTTPS хост дээрх тохиргоо

Тогтмол ажилладаг Node.js 22 эсвэл 24 сервер, эсвэл Next.js-ийн серверийн API дэмждэг хост хэрэгтэй. Static-only hosting тохирохгүй. Хост/домэйн одоогоор сонгож баталгаажуулаагүй; төлбөртэй үйлчилгээ захиалаагүй.

| Тохиргоо | Утга |
|---|---|
| SITE_URL | Яг ашиглах нийтийн HTTPS үндсэн хаяг, замгүй. Жишээ `https://kino.example.com` |
| SUPABASE_URL | Өөрийн Supabase төслийн HTTPS хаяг |
| SUPABASE_SERVICE_ROLE_KEY эсвэл SUPABASE_SECRET_KEY | Серверийн шинэ secret key |
| ADMIN_PASSWORD | 16-аас доошгүй тэмдэгттэй шинэ нууц үг |
| SMS_WEBHOOK_SECRET | 32-оос доошгүй тэмдэгттэй санамсаргүй нууц |
| SMS_ALLOWED_SENDER | `Khan Bank` — бодит SMS илгээгчтэй яг таарна |
| MESSENGER_URL | Шаардлагатай бол өөрийн хуудасны HTTPS холбоос |

Эдгээр нууцыг hosting-ийн environment variable хэсэгт тохируулна. `.env.local`-ийг GitHub, ZIP, чат руу оруулахгүй. Өмнө зурагт харагдсан service key, админы нууц үг, SMS нууцыг нийтийн хост дээр гаргахаас өмнө солино. SMS нууцыг солихдоо MacroDroid-ийн Authorization утгыг мөн шинэчилнэ. SITE_URL-ийг **build хийхээс өмнө** тохируулна; домэйн соливол дахин build/deploy хийнэ.

```powershell
npm run preflight
npm run build
npm start
```

`preflight` нь зөвхөн орчны тохиргооны хэлбэрийг шалгадаг. Түлхүүр хүчинтэй эсэх, бодит TLS, SMS, SQL-ийн ажиллагааг батлахгүй. LAN dev дээр HTTP SITE_URL-ийн HTTPS шалгалт FAIL гарах нь хэвийн; энэ шалгалт HTTPS хостод зориулагдсан.

## VPS ашиглахаар сонговол бэлэн загвар

`Dockerfile`, `compose.yaml`, `deploy/Caddyfile` оруулсан. Энэ загвар энд Docker-оор ажиллуулж туршигдаагүй; хост дээр туршиж баталгаажуулна.

- Домэйны A/AAAA бичлэгийг VPS руу чиглүүлнэ. 80/443 порт гаднаас хүрдэг байна.
- Хост дээр `.env.production` файл үүсгэж дээрх хувьсагчдыг бөглөнө. Зөвхөн өөрийн серверт хадгална.
- Node-ийн 3000 портыг гаднаас шууд нээхгүй; compose дотор зөвхөн Caddy холбогдоно.
- Төслийн хавтсанд:

```bash
docker compose --env-file .env.production config --quiet
docker compose --env-file .env.production up -d --build
```

Caddy нь нөхцөл бүрдсэн үед HTTPS сертификат авч шинэчилж, HTTP-ээс HTTPS руу чиглүүлдэг. `caddy_data` volume-ийг хадгална.

Эх сурвалж: [Caddy automatic HTTPS](https://caddyserver.com/docs/automatic-https), [Next.js deployment](https://nextjs.org/docs/app/getting-started/deploying).

## MacroDroid-ийг шинэ хаягт холбох

| Талбар | Утга |
|---|---|
| Trigger | SMS Received, `Khan Bank`, Contains `ORLOGO` |
| Method | POST |
| URL | Шинэ бодит HTTPS хаяг + `/api/sms` |
| Content type | `text/plain` |
| Body | `{sms_message}` |
| Authorization | `Bearer ` + серверийн SMS_WEBHOOK_SECRET |
| X-SMS-Sender | `{sms_number}` |

Шинэ URL үнэхээр ажилласны дараа MacroDroid-ийн хуучин LAN хаягийг солино. Хоёр макро зэрэг дамжуулах шаардлагагүй. Сертификат шалгах хамгаалалтыг унтраахгүй.

## Нийтэд нээхээс өмнөх бодит шалгалт

1. Админ → Тохиргоо → **HTTPS бэлтгэл шалгах**. Шалгалт дутуу мөрийг шийднэ. Бүх мөр ✓ байсан ч дараах бодит шалгалтууд шаардлагатай.
2. Нэвтрээгүй хэрэглэгч төлбөртэй кино нээж чадахгүй; үнэгүй кино нээгдэнэ.
3. Бүртгүүлж нэвтрэх, гарах, хуудсаа шинэчлэх, дахин нэвтрэх ажиллагаа.
4. Нэг кино, нэг ангиллын багц, бүх ангиллын багц зөв үнэ/эрхээр нээгдэх.
5. Шинэ захиалгын 6 оронтой кодоор бодит орлогын SMS ирээд **POST /api/sms 200** болон `{"ok":true}` хариутай болох. Энэ бол серверийн хариу; хэрэглэгчийн кино мөн нээгдсэн эсэхийг харна.
6. Нөгөө хэрэглэгчийн данснаас тэр кино нээгдэхгүй байх. Админ нэг захиалгын эрх хасахад бусад эрх хэвээр байх.
7. Утас сүлжээгүй байгаад 4 цагийн дараа бодит SMS ирэх туршилт. Код/дүн зөв, захиалга үүссэнээс 24 цагийн дотор дамжсан бол эрх баталгаажсан мөчөөс эхэлнэ.
8. **SMS ирсэн боловч дата байхгүй үед** HTTP хүсэлт алдагдаж болохыг тусад нь туршина. Одоогийн MacroDroid тохиргоонд баталгаатай хадгалсан дараалал/дахин илгээх механизм хийгдээгүй. Серверт хүрээгүй хүсэлтийг апп өөрөө сэргээхгүй. Алдсан SMS-ийг дахин илгээх эсвэл банкны орлогоо шалгаад админ батална. Энэ хувилбарт MacroDroid өөрөө шинэчлэгдэхгүй.
9. Зураг нэмэх/солих, хадгалахын өмнөх тасалдал, дахин оролдох; өөр утсаар шинэ зураг харагдах.
10. Bunny дээр яг шинэ домэйноос тоглож байгаа эсэх, гаднаас хуулсан video/embed URL эрхгүйгээр нээгдэж байгаа эсэх. Апп API дээр эрх шалгадаг боловч гаднын нээлттэй видео URL-ийг HTTPS өөрөө хамгаалахгүй. Bunny-ийн token/domain хамгаалалт болон бодит тохиргоог тусад нь баталгаажуулна.

Утасны дугаарын эзэмшлийг OTP-оор батлах, автомат PIN сэргээх үйлчилгээ энэ хувилбарт холбогдоогүй. 4 оронтой PIN + серверийн оролдлогын хязгаар хэвээр. Hosting-ийн rate limit/monitoring, Supabase Usage хяналт, нөөцлөлтөө бодит хост дээр тохируулна.
