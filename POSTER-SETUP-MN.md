# Киноны зураг — 2026-09-12.1

Шинэ хувилбар зураг сонгоход сервер дээр 600×900 хэмжээтэй WebP болгож, 200,000 byte-аас хэтрүүлэхгүй хадгална. Зургийг сунгах, гарчгийг нь тайрахгүй; хар хүрээгээр бүтнээр нь багтаана. PNG, JPEG, WebP, GIF (эхний кадр), 4 MB хүртэл, 24 сая pixel хүртэл оролт дэмжинэ. HEIC, SVG дэмжихгүй.

## Нэг удаагийн суулгалт

1. Өгөгдлийн сангийн нөөцтэй эсэхээ шалгана.
2. Supabase → SQL Editor → New query нээнэ.
3. Энэ багцын `supabase/HTTPS-UPGRADE.sql` файлын бүх текстийг хуулж Run дарна. Энэ нь өмнөх reviewed хувилбарын SQL суусан төсөлд зориулагдсан. Шинэ хоосон төсөлд эхлээд `supabase/review-install.sql`, дараа нь `HTTPS-UPGRADE.sql` ажиллуулна.
4. SQL дууссаны дараа Storage дотор `kino-posters` гэсэн Public сан байна. Нийтэд upload/delete эрх нэмж болохгүй. Зөвхөн сайтын сервер админы session шалгаад байршуулдаг.
5. Кодоо шинэчилсэн хавтсанд `npm install` ажиллуулна. Серверээ дахин асаана.
6. Сайт → Админ → Кино нэмэх/Засах → Киноны нүүр зураг → файл сонгоно.
7. «Зураг бэлэн» болон урьдчилсан зураг харагдсаны дараа **Хадгалах** дарна.

Нууц түлхүүрийг чат, код, NEXT_PUBLIC хувьсагчид бичихгүй. Одоо байгаа `SUPABASE_SERVICE_ROLE_KEY` хувьсагч хуучин JWT болон шинэ `sb_secret_...` түлхүүрийг хоёуланг дэмжинэ. Шинэ нэр `SUPABASE_SECRET_KEY` мөн дэмжинэ; хоёуланг бөглөсөн бол шинэ нэрийг ашиглана.

## Холболт тасарвал

Байршуулалт амжилтгүй бол киноны өмнөх зураг солигдохгүй. «Дахин оролдох» дарна. Ижил зураг ижил нэрээр хадгалагдах тул хариу алдагдсан хүсэлтийг давтахад дахин дахин зай эзлэхгүй. Өөр зураг өөр нэртэй тул хуучин кэш шинэ зургийг халхлахгүй.

Зураг байршуулсны дараа киноны «Хадгалах» хүсэлт бүтэлгүйтвэл байршуулсан зургийн холбоос формд үлдэнэ; киноны хадгалалтаа дахин оролдоно. Хуучин файл автоматаар устахгүй. Формыг хадгалалгүй хаасан бол ашиглаагүй зураг Storage-д үлдэж болно. Ашиглагдаж байгаа зургийг устгах эрсдэлээс сэргийлж автоматаар цэвэрлэх үйлдэл оруулаагүй.

## Өмнөх том зургуудыг шилжүүлэх

Одоо байгаа зургуудыг автоматаар бөөнөөр өөрчлөөгүй. Админ кино засаж хадгалахад тухайн base64 зураг Storage руу шилжинэ. Олон хуучин зурагтай бол:

1. Ажиллаж байгаа төслийн үндсэн хавтсанд Terminal / PowerShell нээнэ.
2. Эхлээд зөвхөн тоог шалгана:

```powershell
npm run posters:migrate
```

3. Бүх хуучин base64 зургийг нөөцлөж шилжүүлэх команд:

```powershell
npm run posters:migrate -- --apply
```

Нөөцүүд `poster-backups` хавтсанд хадгалагдана. Энэ хавтсыг хадгалж аваарай. Амжилтгүй зурагт хуучин өгөгдөл хэвээр; дахин ажиллуулж болно. Өөр админ зэрэг зураг сольсон бол тэр өөрчлөлтийг дарж бичихгүй. Гаднын HTTPS холбоосоор байгаа зургуудыг татаж шилжүүлэхгүй; ийм зургийг өөрийн эх файлаар дахин оруулбал илүү найдвартай.

## Багтаамж

1,000 × 200 KB ≈ 200 MB. Бодит зургууд үүнээс бага байж болно. Supabase-ийн **Storage хэмжээ** ба **сарын egress** хоёрыг Usage хэсэгт хянана. Киноны видео файл энэ санд оруулахгүй; Bunny дээрх видеонууд хэвээр.

Эх сурвалж: [Supabase buckets](https://supabase.com/docs/guides/storage/buckets/creating-buckets), [standard uploads](https://supabase.com/docs/guides/storage/uploads/standard-uploads).
