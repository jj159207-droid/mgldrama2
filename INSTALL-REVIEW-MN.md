ӨМНӨХ ХУВИЛБАРЫН ТАЙЛБАР: 2026-09-12.1 хувилбарыг суулгахдаа HTTPS-READY-MN.md-ийг дагана. Доорх хуучин суулгах алхам, INSTALL.cmd, SQL шаардлагагүй гэсэн тайлбар, hash-ийг шинэ багцтай бүү холь.

# Шалгалтын засварыг суулгах

Энэ багц: `kino-review-update-20260911.zip`. Өмнөх `kino-site-update` / `kino-access-fix` хавтастай бүү холь. Татсан ZIP-ээ шинэ хавтас руу Extract all хийж задлана.

1. `npm run dev` ажиллаж байгаа хар цонхонд Ctrl+C дарж зогсооно. `Terminate batch job (Y/N)?` гэвэл Y → Enter.
2. Өөрийн Supabase өгөгдлийн сангийн нөөцийг авна. Нэгдсэн SQL нь хуучин шууд Supabase ашигладаг deployment-ийн хандалтыг хаана; тухайн deployment ажиллаж байгаа бол шинэ серверийн хувилбарт шилжүүлэх хугацаатай тааруулж хийнэ.
3. Шинэ задласан хавтсан дотор `INSTALL.cmd` ажиллуулна. Файлуудыг нөөцлөөд одоогийн төсөл рүү хуулна. `.env.local`-ийг өөрчлөхгүй. Нууц утгаа чат руу явуулахгүй.
4. SUCCESS гарсны дараа **энэ шинэ багцын** `supabase/review-install.sql`-ийг Notepad-аар нээнэ. Бүгдийг хуулж Supabase → SQL Editor → New query-д тавиад Run дарна. Нэг transaction тул алдаа гарвал бүх SQL өөрчлөлт буцна; алдаатай бол серверээ асааж туршихаас өмнө алдааг засна. Давхардсан өгөгдлийг автоматаар устгахгүй. Кино/хэрэглэгч/төлбөрийн мөрүүдийг устгахгүй.
5. SQL амжилттай бол дараах командыг CMD дээр ажиллуулна:

```bat
cd /d "D:\New folder (3)\Downloads\mgldrama2-reviewed\mgldrama2-reviewed"
npm run dev
```

`Ready` гармагц localhost:3000 нээнэ. Энэ хар цонхыг ажиллаж байхад нь хаахгүй. Ctrl+Shift+R дарж хуучин хуудсаа шинэчилнэ.

6. Туршилтын хэрэглэгчээр нэг кино/багц нээх, админаас зөвхөн нэг эрх хасах, өөр эрх хэвээр байх, дахин худалдан авах, буцаад нэвтрэхийг шалгана.
7. `supabase/verify-readonly.sql` нь өөрчлөлт хийхгүй. Бүх query-ийн үр дүнг шалгаж бодит сангийн хамгаалалтыг баталгаажуулна. Түлхүүр/PIN/киноны холбоосыг гаргахгүй. Баганын MISSING эсвэл permission true гэсэн үр дүнг тайлбарлуулах шаардлагатай байж болно.

## INSTALL.cmd цонх хаагдвал
Задласан шинэ хавтсаа нээгээд хаягийн мөр дээр Ctrl+L → `cmd` → Enter. Хар цонхонд:

```bat
INSTALL.cmd
```

Төслийн байрлал өөр бол PowerShell дээр замаа өгч ажиллуулж болно:

```powershell
.\INSTALL.ps1 -ProjectRoot 'D:\өөр-хавтас\mgldrama2-reviewed'
```

Скрипт зөвхөн заасан файлуудыг хуулна. Нөөцийн зам SUCCESS мөрөнд гарна. Хуулах явцад алдаа гарвал хуулж эхэлсэн файлуудыг өмнөх төлөвт сэргээхийг оролдоно. SQL болон env нь файлын нөөцөөс тусдаа; SQL-ийг хуучин өгөгдлийн сангийн эрхийг нээж буцаах команд бэлдээгүй.

Бүтэн `mgldrama2-reviewed.zip` нь нэмэлт бүтэн эх кодын нөөц. Энэ засварыг суулгахын тулд хоёр ZIP-ийг зэрэг задлаж хуулж холих шаардлагагүй.

## Бодит холболтын үлдсэн шалгалт
Энэ суулгагч Vercel дээр deployment хийхгүй, төлбөр төлөхгүй, Bunny/MacroDroid тохиргоонд хандахгүй. Тэдгээрийн төлөв, бодит утасны туршилт `REVIEW-MN.md` тайланд үлдсэн ажлаар тэмдэглэгдсэн.
